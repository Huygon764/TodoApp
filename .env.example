# MongoDB Atlas (same cluster as diary app, different database)
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/todo-app

# JWT (httpOnly cookie)
JWT_SECRET=your_super_secure_random_string
JWT_EXPIRES_IN=30d

# CORS - frontend origin (production: same origin or your domain)
FRONTEND_URL=http://localhost:3000

# Security
BCRYPT_ROUNDS=12

# Telegram (admin: /register, /remove, /list)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
TELEGRAM_WEBHOOK_DOMAIN=

# Server
PORT=3000

# API key
GEMINI_API_KEY=
