# MongoDB Atlas (use same cluster, different database name)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/todo-app

# JWT (httpOnly cookie)
JWT_SECRET=your_super_secure_random_string_here
JWT_EXPIRES_IN=30d

# CORS - frontend origin
FRONTEND_URL=http://localhost:5173

# Security
BCRYPT_ROUNDS=12

# Telegram (for /register, /remove, /list - admin only)
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
TELEGRAM_WEBHOOK_DOMAIN=

# Server
PORT=5000

# API key
GEMINI_API_KEY=

# Google OAuth (Sign in with Google). Public value, not a secret.
# Backend verifies the Google ID token's audience against this.
GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com
